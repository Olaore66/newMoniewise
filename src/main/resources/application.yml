spring:
  datasource:
    hikari:
      # Keep app instances * max pool size + admin/jobs/Flyway below the DB max.
      maximum-pool-size: ${HIKARI_MAX_POOL_SIZE:40}
      connection-timeout: ${HIKARI_CONNECTION_TIMEOUT_MS:45000}
